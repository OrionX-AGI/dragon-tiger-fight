# 依赖清单

> 本文件是给人阅读的依赖说明。**实际安装依据的是 `package.json` 与 `package-lock.json`**，
> 它们在 Node 生态中相当于 Python 的 `requirements.txt`（`package.json` 声明版本范围，
> `package-lock.json` 锁定精确版本以保证任何机器上装出完全一致的依赖树）。
> 新增依赖请用 `npm install 包名`，npm 会自动同步这两个文件，不要手工编辑。

## 运行环境

| 项目 | 要求 |
|---|---|
| Node.js | 18 或更高（推荐 LTS，当前开发环境 v24） |
| npm | 随 Node.js 一起安装（当前开发环境 11.x） |
| 操作系统 | Windows / macOS / Linux 均可（启动脚本为 Windows 版） |

## 运行时依赖（dependencies）

游戏运行必需，会被打包进最终产物。

| 依赖 | 版本 | 用途 |
|---|---|---|
| react | ^19.2.8 | 界面框架，负责所有游戏界面的渲染与状态驱动 |
| react-dom | ^19.2.8 | React 的浏览器渲染器 |

> 本分支（小红书适配）刻意只保留了 react / react-dom 两个运行时依赖。
> 原先规则页用的 `react-markdown` + `remark-gfm` 已移除：它们的依赖链在顶层用
> `RegExp()` 构造了 `\p{ID_Start}` 与 lookbehind 正则，小工具的 Chrome 61 基线
> 在构造时就会抛 SyntaxError，导致整包初始化失败。改为构建期用 `marked`
> 把规则 Markdown 预渲染成 HTML（见下方开发依赖），运行时零成本，
> 顺带让 JS 产物从 395 KB 降到 244 KB。

## 开发依赖（devDependencies）

仅开发、测试、构建时使用，不进入最终产物。

| 依赖 | 版本 | 用途 |
|---|---|---|
| vite | ^8.2.1 | 开发服务器与生产构建工具 |
| @vitejs/plugin-react | ^6.0.5 | Vite 的 React 支持（JSX 编译、热更新） |
| typescript | ^7.0.2 | 类型检查（`npm run build` 会先跑 `tsc --noEmit`） |
| @types/react | ^19.2.18 | React 的类型定义 |
| @types/react-dom | ^19.2.4 | React DOM 的类型定义 |
| vitest | ^4.1.10 | 单元测试框架，运行规则引擎与 AI 的测试 |
| sharp | ^0.35.3 | 图片处理：打包小工具时按手机尺寸瘦身牌面图（`scripts/build-xhs.mjs`）、生成上传图标（`scripts/make-icons.mjs`）、压缩截图 |
| marked | ^17.1.0 | 构建期把规则 Markdown 渲染成 HTML 字符串（`vite.config.ts` 的 `markdownAsHtml` 插件），不进入运行时产物 |

### 不在 package.json 中的临时工具

`scripts/screenshots.mjs`（桌面截图）与 `scripts/screenshots-mobile.mjs`（手机竖屏
布局核对）需要 `playwright-core` 驱动系统自带的 Edge。它们只在更新截图或核对适配
时用得上，因此不作为项目依赖长期保留，按需临时安装即可（脚本头部有完整用法）：

```bash
npm i --no-save playwright-core
```

## 常用命令

```bash
npm install       # 安装依赖（一键启动脚本会自动判断是否需要执行）
npm start         # 启动游戏并自动打开浏览器
npm run dev       # 启动开发服务器（不自动开浏览器）
npm test          # 运行全部单元测试
npm run build     # 类型检查 + 生产构建，产物在 dist/
npm run preview   # 本地预览生产构建产物
npm run build:xhs # 打小红书小工具离线包：构建 → 图片瘦身 → 合规自检 → 官方审计 → 压 zip
```

## 关于 .npmrc

本机默认的 npm 缓存目录（`D:\AICodingSoftware\nodejs\node_cache`）存在 EPERM 权限问题，
会导致 `npm install` 失败。项目根目录的 `.npmrc` 已将缓存改到用户目录下的
`%LOCALAPPDATA%\npm-cache-dgc`，无需额外操作。换机器开发时若无此问题，可删除该文件。
