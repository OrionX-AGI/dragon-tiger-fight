# 小红书小工具适配说明

本分支（`xiaohongshu`）是《龙虎斗》为小红书小工具平台做的适配版本，从 `main`
分支（纯人机对战版）派生。玩法、规则、AI 强度与 `main` 完全一致，**只改运行
环境相关的部分**。

适配依据是官方 `minitool-zip-builder` 1.6.0 skill，已解压在
`.claude/minitool-zip-builder/`（`build:xhs` 的官方审计步骤会调用它）。

**结论**：`npm run build:xhs` 产出合规离线包 **1.01 MB**，官方审计脚本对产物
目录与 zip 均返回 PASS，自建的 30 余项合规自检全部通过。

---

## 一、下次开新项目：第一天就该定的规矩

这次是先做完 PC 网页版再适配，导致布局几乎全部返工、`react-markdown` 整条链
被迫拆除。**如果一开始就打算上小工具平台，下面这些从第一行代码就定下来，能省
掉整轮返工。**

### 技术选型

1. **构建目标直接写 `target: 'chrome61'`、`format: 'iife'`、`base: './'`**，
   不要等适配时再改。语法能转译，运行时 API 和正则不能，早暴露早换掉。
2. **引入任何依赖前先查它的运行时下限**。重点查两件事：模块顶层有没有构造
   带 `\p{...}`（需 Chrome 64）或 lookbehind `(?<=)`（需 Chrome 62）的正则；
   有没有用 `Object.hasOwn`、`Object.fromEntries`、`globalThis`、`structuredClone`。
   顶层执行的东西一旦抛错，**整个 bundle 初始化失败，用户看到全白页面**，
   连错误提示都没有，排查成本极高。
3. **静态内容一律构建期处理，不要运行时解析**。Markdown、配置、模板都在构建期
   转成字符串塞进产物，既避开解析库的兼容性地雷，又省体积（本项目 JS 从 395 KB
   降到 244 KB）。
4. **不要用 Web Worker**。容器禁用，重计算只能放主线程；提前设计好时间预算和
   让出一帧的时机，而不是最后再拆 Worker。
5. **音频用 Web Audio 运行时合成，不用媒体文件**。容器 CSP 禁 `data:`/`blob:`
   媒体源，且离线包体积紧张。见第五节。

### 布局

6. **一开始就按手机竖屏做，PC 端当成宽屏适配**，反过来一定返工。
7. **CSS 不用 `gap`**（flex 的 gap 要 Chrome 84）。项目起手就统一用「子项
   margin」造间距，别等到有几百条规则时再逐条替换。
8. **每个页面都定一条"一屏看完"的硬指标**，并且**写成自动断言**而不是靠眼睛看。
   本项目的 `scripts/screenshots-mobile.mjs` 在两个视口下逐页断言横向溢出、
   文案折行、元素对齐，改样式后跑一遍就知道有没有踩坏别处——手工核对根本
   来不及。

---

## 二、平台硬性约束与对应改动

| 平台约束 | 原实现 | 改动 |
|---|---|---|
| 容器禁用 Web Worker | AI 计算跑在 `aiWorker.ts` | 删除 Worker，`aiClient.ts` 改主线程计算，计算前先让出一帧，保证"思考中"提示先绘制 |
| 入口必须是经典脚本，不能有 `type="module"` | Vite 默认输出 module 脚本 | 输出格式改 `iife`，用 `classicScriptTag` 插件去掉 `type="module"`/`crossorigin`，并把脚本从 `<head>` 移到 `</body>` 前 |
| 离线包内一律相对路径 | 图片写死 `/assets/...` | 牌面路径改 `./assets/...`；棋盘底纹移入 `src/assets/` 交由构建改写；`base` 设为 `./` |
| 兼容基线 Chrome 61 | 见第三节 | 见第三节 |
| 不得有任何外部网络请求 | 本就无 | 自检扫描 `src`/`href`/`url()` 外链，确认为 0 |
| zip 根目录必须是 `index.html` | — | 自研 zip 写入器压缩 `dist` 的**内容**而非目录本身 |
| 建议体积 2 MB 内 | 牌面图 640px 共 3.2 MB | 打包时按手机展示尺寸缩到 360px，降至 0.89 MB，整包 1.01 MB |

**脚本位置的坑**：去掉 `type="module"` 后脚本同时失去了隐式 `defer`，留在
`<head>` 会在 `#root` 存在之前执行，React 挂载失败。必须同时把脚本移到
`</body>` 之前。

---

## 三、Chrome 61 基线

基线是 Android 8.1 出场的 Chrome / WebView 61。语法由 Vite 转译解决，
**运行时 API 和正则不会被转译**，这才是风险点。

### 白屏级问题：`react-markdown` + `remark-gfm` 整条链不可用

`property-information` 在模块顶层执行 `RegExp('[$_\\p{ID_Start}]', 'u')`，
`remark-gfm` 的自动链接用了 lookbehind。两者分别需要 Chrome 64 / 62，在 61 上
构造即抛 `SyntaxError`；因为在顶层执行，整个 IIFE 初始化失败，**表现是全白页面
而不是规则页报错**。该链还调用了 `Object.hasOwn`（Chrome 93）。

解法：规则文档是随包固定的静态内容，没必要运行时解析。改为构建期用 `marked`
预渲染成 HTML 字符串（`vite.config.ts` 的 `markdownAsHtml` 插件），页面用
`dangerouslySetInnerHTML` 输出。`marked` 只是开发依赖，不进产物。

### 主动替换掉的 API

| API | 最低版本 | 替换为 |
|---|---|---|
| `globalThis` | Chrome 71 | `typeof window !== 'undefined'` 判断后用 `window` |
| `Object.fromEntries` | Chrome 73 | `forEach` 手工填对象 |

产物里还剩 `structuredClone`、`queueMicrotask`、`AbortController`、
`navigator.connection`，都在 React 内部且带存在性判断，取不到走回退分支，不抛错。

---

## 四、CSS 基线改写

`src/styles.css` 顶部注释列了完整对照。核心是几个比基线晚很多才落地的能力：

| 能力 | 最低版本 | 替换为 |
|---|---|---|
| flex 的 `gap` | Chrome 84 | 子项 `margin`；会换行的容器用「容器负外边距 + 子项外边距」 |
| grid 的 `gap` | Chrome 66 | `grid-gap` |
| `aspect-ratio` | Chrome 88 | 显式写宽高（3:4 手工算好） |
| `inset` 简写 | Chrome 87 | `top`/`right`/`bottom`/`left` 四条 |
| `backdrop-filter` | Chrome 76 | 半透明实色背景作基线，`@supports` 内再加模糊 |

另外补齐移动端该有的：`-webkit-tap-highlight-color`、`-webkit-touch-callout`、
`touch-action: manipulation`、`user-select` 的 `-webkit-` 前缀，`:hover` 统一收进
`@media (hover: hover)` 并补 `:active` 按下反馈，安全区用
`var(--safe-area-inset-*, env(safe-area-inset-*, 0px))` 双层回退。

**负外边距的坑**：用「容器负外边距」模拟 gap 时，负值不能超过父容器的横向
内边距，否则整页横向溢出。窄屏下 `.game-screen` 内边距降到 10px 而
`.board-wrap` 负边距是 11px，实测溢出 1px。

---

## 五、竖屏适配的通用教训

`viewport` 加了 `viewport-fit=cover` 与 `maximum-scale=1.0, user-scalable=no`。
断点分 720px（手机竖屏）、400px（iPhone SE 一档）两档，另加一档
`max-height: 700px`——**宽度和高度是两个独立维度，只按宽度分档会让矮屏白白
浪费横向空间**。`main.tsx` 用 `visualViewport` 维护 `--app-height`，CSS 里
始终保留 `100vh` 回退，脚本没跑到时布局依然成立。

三轮返工提炼出的通用经验，下次直接照做：

1. **左中右三块并排的信息行，在手机上必然折成三行。** 改成"两块一行 + 第三块
   独占一行居中"，并把每块的文案压到最短（座次用前缀表达，已在别处选过的
   选项不再重复显示）。
2. **夹在两个元素中间的文字区只有一百多像素宽，任何一句话都会折行。** 把它移出
   到整宽的独立行并加 `white-space: nowrap`。
3. **同一位置上互斥出现的主操作，合并到一个底部固定操作栏。** 不要每个阶段
   在页面中央各放一个按钮。
4. **倒计时文案并入按钮括号内**（`开始下一局（3）`），省掉整行。
5. **所有结算/提示面板都用悬浮 overlay**，不要占文档流高度。
6. **容器在顶栏右上角预置了分享按钮**，那个位置不能放自己的按钮。三列网格顶栏
   要保留一个空占位元素，否则少一列标题就压不住中线。
7. **面板尺寸要由内容尺寸参数化算出来**（本项目用 `--cell` / `--board-w`
   推导损失面板宽度），写死的 `min-height` 一定会在某个断点被内容撑破，
   看起来像元素重叠。
8. **空面板不要渲染**，开局摆两个空框纯占高度。
9. **CSS 优先级要靠类数量而不是靠顺序。** 媒体查询里的同优先级规则排在后面
   会赢：本项目 `.topbar > * + * { margin-left: 8px }` 把标题挤偏 4px，改用
   `.topbar.topbar-centered > * + *` 两个类才压住。
10. **全角标点在 flex 里居不了中。** 全角「？」字形在字符框内靠左、右侧留白，
    flex 居中的是字符框，视觉上就是偏的，换半角 `?`。
11. **固定宽度的标签容器装不下更长的文案。** 两字标签定的 42px 宽，遇到四字
    的「背景音乐」就折行，设置类页面要放开宽度并 `white-space: nowrap`。

---

## 六、声音系统（零媒体文件）

BGM 与六种音效全部由 `src/audio/sound.ts` 在运行时用 Web Audio 合成，包里
没有任何音频文件。原因：

- 一段 1 分钟 BGM 压成 mp3 也要 0.7-1 MB，会把包体从 1.01 MB 直推向 2 MB 建议线；
- 容器 CSP 禁止 `data:` / `blob:` 媒体源，运行时生成的音频没法喂给 `<audio>`，
  而 Web Audio 不经过媒体元素，不受该限制；
- 自己合成的波形无任何第三方授权问题。

实现取舍：

- 所有音色在首次用户手势时**预渲染进 AudioBuffer**（拨弦用 Karplus-Strong
  算法，其余逐采样加法合成），之后播放只是 buffer 调度。刻意不用
  `ScriptProcessorNode` 实时生成——棋盘困难 AI 会阻塞主线程约 1 秒，实时路径
  必爆音。
- BGM 是 A 羽调五声音阶的 64 拍循环，84 BPM、基本一拍一音。首版 66 BPM 且
  大量空拍，实测过于缓慢低沉。调度器提前 3.2 秒排音符，AI 占住主线程时音乐不断。
- **音效设计走过一次弯路**：首版用合成的"龙吟""虎啸"拟声配吃子，实测很难听——
  加法合成模拟兽吼的失真感在手机扬声器上只剩噪音。现版全部改为简短乐音：
  龙方吃子是三连升调铃音、虎方是两连降调木鱼声（一升一降好分辨）；终局不按
  阵营播吼声，而按**玩家胜负**播军号胜利号 / 下行失落乐句。
  **经验：短促、干净、音高有差异的乐音，永远比拟声好听且好分辨。**
- 自动播放策略：`main.tsx` 监听 `pointerdown` 调 `ensureAudio()`，首次手势建
  `AudioContext`，后续手势负责唤醒被容器挂起的上下文；`visibilitychange` 时
  挂起/恢复，符合平台「不可见时停媒体」的性能规范。
- `AudioContext` 不在平台禁用能力清单内，Chrome 61 走 `webkitAudioContext` 回退。
- 音乐、音效开关在独立设置页，偏好存 localStorage。

---

## 七、上传用图标

平台以「图标 + 标题」展示小工具，需要 1:1 方形图标，素材在 `docs/icon/`：
`icon-1024.png` 是母版，其余尺寸（512 / 256 / 192 / 128 / 64）都从它缩放，
另有 `icon-512.webp` 和 `icon-preview-masked.png`（22% 圆角遮罩预览，用来确认
加圆角后的裁切效果）。重新生成：`node scripts/make-icons.mjs`。

图标**不打进离线 zip**——平台从上传页读取，放进包里只会白占体积。
设计上刻意对齐游戏内美术风格，构图对称、主体轮廓粗重，保证 128px 缩略图下仍
可辨识。

---

## 八、打包与校验

```bash
npm run build:xhs
```

流程：`vite build` → 牌面图瘦身 → 结构与合规自检 → 官方审计 `dist` → 压 zip
→ 官方审计 zip → 输出校验摘要。任一项不通过即以非零码退出。产物固定生成在
`output/龙虎斗-小红书小工具.zip`（`output/` 不入库）。

自检脚本（`scripts/build-xhs.mjs`）覆盖：入口位置、文件类型白名单、无 source
map、`index.html` 的 DOCTYPE / lang / charset / viewport、无 `type="module"`、
无内联脚本、无行内事件、无 `<base>` / `<iframe>` / 自建 CSP、无绝对路径、
引用文件真实存在、入口脚本排在 `#root` 之后、28 项禁用能力扫描、11 项
Chrome 61 不兼容写法扫描、外链扫描。

zip 由 `scripts/zip-writer.mjs` 生成，自己实现而非引第三方库，是为了完全控制
条目路径——一律 `/` 分隔且不带顶层目录，保证解压后 `index.html` 直接位于根目录。

### 布局核对

```bash
npm i --no-save playwright-core
npm run build:xhs && npx vite preview --port 4173 --host 127.0.0.1 --strictPort
node scripts/screenshots-mobile.mjs
```

在 390×844 与 360×640 两个视口下走完大厅、纸牌（记牌 / 揭示 / 弃牌三态）、
棋盘、规则、设置六个界面，逐页断言，任一不满足即以非零码退出：

| 断言 | 判据 |
|---|---|
| 无横向溢出 | `document.scrollWidth <= 视口宽` |
| 纵向是否一屏可见 | `document.scrollHeight` 与视口高之差（仅报告，不作硬失败） |
| 关键文案不折行 | 行内元素数 `getClientRects().length`；块级元素按「内容高 ÷ 行高」，需扣掉自身 padding，否则 padding 会被误判成第二行 |
| 损失面板与棋盘同宽 | `.captured-col` 与 `.board-grid` 的 `offsetWidth` 相等 |
| 标题与战绩压在棋盘中线 | 与 `.board-grid` 的中心 x 之差 ≤ 1px |
| 空牌位问号左右居中 | `.card-empty-mark` 与 `.card-empty` 的中心 x 之差 ≤ 1px |
| 设置项开关贴右 | `.opt-group` 右边缘与面板内容区右边缘之差 ≤ 5px |

截图输出到 `docs/screenshots-mobile/`。纵向实测：大厅、规则、设置在两个视口
都一屏可见；棋盘 390 一屏可见、360 需滑 1px；纸牌页 360×640 需滑约 137px。

纸牌页装不进 360×640 是结构性的：两副 2×4 手牌加中央出牌区，纵向最少要七百多
像素。继续压缩只能把牌缩到看不清牌面，不划算，因此停在这里。

---

## 九、尚未实测的部分

按官方规范如实标注，以下只做了静态核对，没有真机数据：

- **Chrome 61 / Android 8.1 真机兼容性未实测。** 兼容性结论来自对产物的静态
  扫描与版本比对，没有在真实低版本设备或对应内核上跑过。
- **真机帧率与首屏耗时未实测。**
- **AI 思考期间的主线程占用。** 容器禁用 Worker，棋盘困难 AI 的搜索在主线程
  执行，受墙钟预算封顶约 950 ms（低端机上只会搜得更浅，不会耗时更久），期间
  界面无响应。计算前会先让出一帧让"电脑思考中"提示绘制出来。这里刻意没有削减
  搜索预算，以保持与 `main` 分支一致的 AI 强度；如果真机体感不佳，可下调
  `src/ai/boardAI.ts` 里 `CONFIGS.hard.timeMs`。
