# 小红书小工具适配说明

本分支（`xiaohongshu`）是《龙虎斗》为小红书小工具平台做的适配版本，从 `main`
分支（纯人机对战版）派生。游戏玩法、规则、AI 强度与 `main` 完全一致，**只改
运行环境相关的部分**，所有改动都能在下面找到对应的平台要求。

适配依据是官方的 `minitool-zip-builder` 1.6.0 skill，已解压在 `.claude/minitool-zip-builder/`。

## 一句话结论

`npm run build:xhs` 可产出合规离线包 **1.01 MB**，官方审计脚本对产物目录与 zip
均返回 PASS，自建的 30 余项合规自检全部通过。

## 平台的硬性约束与本项目的对应改动

| 平台约束 | 原实现 | 改动 |
|---|---|---|
| 容器禁用 Web Worker | AI 计算跑在 `aiWorker.ts` | 删除 Worker，`aiClient.ts` 改为主线程计算，计算前先 `requestAnimationFrame` 让出一帧，保证"思考中"提示先绘制 |
| 入口必须是经典脚本，不能有 `type="module"` | Vite 默认输出 module 脚本 | 输出格式改 `iife`，并用 `classicScriptTag` 插件去掉 `type="module"`/`crossorigin`，同时把脚本从 `<head>` 移到 `</body>` 前 |
| 离线包内一律相对路径 | 图片写死 `/assets/...` | 牌面路径改 `./assets/...`；棋盘底纹移入 `src/assets/` 交由构建改写；`base` 设为 `./` |
| 兼容基线 Chrome 61 | 见下节 | 见下节 |
| 不得有任何外部网络请求 | 本就无 | 自检脚本扫描 `src`/`href`/`url()` 外链，确认为 0 |
| zip 根目录必须是 `index.html` | — | 自研 zip 写入器压缩 `dist` 的**内容**而非目录本身 |
| 建议体积 2 MB 内 | 牌面图 640px 共 3.2 MB | 打包时按手机展示尺寸缩到 360px，降至 0.89 MB，整包 1.01 MB |

## Chrome 61 基线：踩到的坑

基线是 Android 8.1 出场的 Chrome / WebView 61。语法由 Vite 转译（`target: chrome61`）
解决，但**运行时 API 和正则不会被转译**，是真正的风险点。逐项排查结果：

### 必须解决的（会直接白屏）

**`react-markdown` + `remark-gfm` 整条链不可用。** 这是本次适配最严重的问题。
`property-information` 在模块顶层执行 `RegExp('[$_\\p{ID_Start}]', 'u')`，
`remark-gfm` 的自动链接用了 lookbehind `(?<=...)`。Unicode 属性转义要 Chrome 64、
lookbehind 要 Chrome 62，在 61 上构造这两个正则会抛 `SyntaxError`；由于它们在顶层
执行，整个 IIFE 初始化失败，**用户看到的是全白页面，不是规则页出错**。此外该链还
调用了 `Object.hasOwn`（Chrome 93）。

解法：规则文档是随包固定的静态内容，没必要在运行时解析。改为构建期用 `marked`
预渲染成 HTML 字符串（`vite.config.ts` 的 `markdownAsHtml` 插件），页面用
`dangerouslySetInnerHTML` 输出。`marked` 只是开发依赖，不进产物。
附带收益：JS 产物 395 KB → 244 KB。

### 主动替换掉的

| API | 最低版本 | 位置 | 替换为 |
|---|---|---|---|
| `globalThis` | Chrome 71 | `core/logger.ts` 装调试入口 | `typeof window !== 'undefined'` 判断后用 `window` |
| `Object.fromEntries` | Chrome 73 | `ai/cardAI.ts`、`ai/boardAI.ts` 打日志 | `forEach` 手工填对象 |

### 确认安全、保留原样的

这几项在产物里仍有出现，但都带能力检测、取不到时走回退分支，不会抛错：
`structuredClone`、`queueMicrotask`、`AbortController`（React 自带的 polyfill 写法），
以及 `typeof self === 'object' ? self : globalThis` 里那个短路后不会求值的 `globalThis`。
另有 `navigator.connection` 出现在 React 内部的带宽估算里，有存在性判断，
取不到时回退默认值。

## CSS 基线改写

`src/styles.css` 顶部注释列了完整对照。核心是几个比基线晚很多才落地的能力：

| 能力 | 最低版本 | 替换为 |
|---|---|---|
| flex 的 `gap` | Chrome 84 | 子项 `margin`；会换行的容器用「容器负外边距 + 子项外边距」 |
| grid 的 `gap` | Chrome 66 | `grid-gap` |
| `aspect-ratio` | Chrome 88 | 牌面与棋格显式写宽高（3:4 手工算好） |
| `inset` 简写 | Chrome 87 | `top`/`right`/`bottom`/`left` 四条 |
| `backdrop-filter` | Chrome 76 | 半透明实色背景作基线，`@supports` 内再加模糊 |

另外补齐了移动端该有的东西：`-webkit-tap-highlight-color`、`-webkit-touch-callout`、
`touch-action: manipulation`、`user-select` 的 `-webkit-` 前缀，`:hover` 效果统一收进
`@media (hover: hover)` 并补 `:active` 按下反馈，安全区用
`var(--safe-area-inset-*, env(safe-area-inset-*, 0px))` 双层回退。

### 关于负外边距的一个坑

用「容器负外边距」模拟 gap 时，**负值不能超过父容器的横向内边距**，否则整页横向
溢出。窄屏下 `.game-screen` 内边距降到 10px，而 `.board-wrap` 的负边距是 11px，
实测出现 1px 横向溢出。已在窄屏断点把该值降到 5px。

## 竖屏适配

`viewport` 加了 `viewport-fit=cover` 与 `maximum-scale=1.0, user-scalable=no`。
断点分 720px（手机竖屏）与 400px（iPhone SE 一档）两档，主要调整：

- 大厅面板从 `minmax(340px, 1fr)` 自适应网格改为单列——340px 的下限在窄屏会撑出横向滚动
- 手牌 8 张自然折成 2×4（牌宽 76px / 70px）
- 棋盘格 106px → 72px / 66px，损失区从竖排改为横排铺在棋盘上下
- 顶栏字号与间距收紧；按钮文案统一 `white-space: nowrap`，避免"返回大厅"被拆行
- 规则页顶栏在窄屏换行：返回按钮与标题同行，两个标签页独占一行
- 规则里的克制关系表可横向滚动，不撑破页面

`main.tsx` 用 `visualViewport` 维护 `--app-height`，CSS 里始终保留 `100vh` 回退，
脚本没跑到时布局依然成立。

## 上传用图标

平台以「图标 + 标题」的形式展示小工具，需要 1:1 方形图标。素材放在 `docs/icon/`：

| 文件 | 用途 |
|---|---|
| `icon-1024.png` | 母版，1024×1024，其余尺寸都从它缩放 |
| `icon-512.png` | 小工具/小程序上传页最常见的要求尺寸 |
| `icon-256.png` / `icon-192.png` / `icon-128.png` / `icon-64.png` | 其他平台按需选用 |
| `icon-512.webp` | 网页场景用，体积更小 |
| `icon-preview-masked.png` | 圆角遮罩（半径 22%）预览，用来确认加圆角后的裁切效果 |

图标内容是龙王与虎王正面对峙、中间迸出金色火花，背景对角分割为朱红云纹（龙侧）
与墨色山峦（虎侧），外描金色云纹边框。配色与画风刻意对齐游戏内的牌面美术
（做旧纸纹 + 木刻版画质感 + 朱红配金），保证图标与游戏内观感一致。构图对称、
主体轮廓粗重，在 128px 缩略图下仍能分辨龙虎与中央的碰撞。

需要重新生成各尺寸时：

```bash
node scripts/make-icons.mjs
```

图标**不打进离线 zip**——平台从上传页读取图标，放进包里只会白占体积。

## 打包与校验

```bash
npm run build:xhs
```

流程：`vite build` → 牌面图瘦身 → 结构与合规自检 → 官方审计 `dist` → 压 zip →
官方审计 zip → 输出校验摘要。任一项不通过即以非零码退出。

自检脚本（`scripts/build-xhs.mjs`）覆盖的检查：入口位置、文件类型白名单、无
source map、`index.html` 的 DOCTYPE / lang / charset / viewport、无 `type="module"`、
无内联脚本、无行内事件、无 `<base>` / `<iframe>` / 自建 CSP、无绝对路径、引用的
文件真实存在、入口脚本排在 `#root` 之后、28 项禁用能力扫描、11 项 Chrome 61
不兼容写法扫描、外链扫描。

zip 由 `scripts/zip-writer.mjs` 生成，自己实现而非引第三方库，是为了完全控制条目
路径——一律 `/` 分隔且不带顶层目录，保证解压后 `index.html` 直接位于根目录。

## 布局核对

```bash
npm i --no-save playwright-core
npm run build:xhs && npx vite preview --port 4173 --host 127.0.0.1 --strictPort
node scripts/screenshots-mobile.mjs
```

在 390×844 与 360×640 两个视口下走完大厅、纸牌、棋盘、规则四个界面，逐页比对
`document.scrollWidth` 与视口宽度，有横向溢出即以非零码退出。截图输出到
`docs/screenshots-mobile/`。

## 尚未实测的部分

按官方规范如实标注，以下项目只做了静态核对，没有真机数据：

- **Chrome 61 / Android 8.1 真机兼容性未实测。** 所有兼容性结论来自对产物的静态
  扫描与版本比对，没有在真实低版本设备或对应内核上跑过。
- **真机帧率与首屏耗时未实测。**
- **AI 思考期间的主线程占用。** 容器禁用 Worker，棋盘困难 AI 的搜索在主线程执行，
  受墙钟预算封顶约 950ms（低端机上只会搜得更浅，不会耗时更久），期间界面无响应。
  计算前会先让出一帧让"电脑思考中"提示绘制出来。这里刻意没有削减搜索预算，
  以保持与 `main` 分支一致的 AI 强度；如果真机体感不佳，可下调
  `src/ai/boardAI.ts` 里 `CONFIGS.hard.timeMs`。
